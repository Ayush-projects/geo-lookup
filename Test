Cumulative Successful Device Count =
CALCULATE(
    [Successful Device Count],
    FILTER(
        ALLSELECTED('AutopilotEvents'),
        'AutopilotEvents'[DeploymentDate] <= MAX('AutopilotEvents'[DeploymentDate])
    )
)
