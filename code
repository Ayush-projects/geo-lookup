- name: Parse stdout_lines into a dictionary of dictionaries using custom filter
  set_fact:
    last_sync_responses: "{{ script_output.stdout_lines | parse_sync_output }}"

- name: Print the final dictionary output
  debug:
    var: last_sync_responses
