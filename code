- name: Merge dictionaries using custom filter
  set_fact:
    combined_data: "{{ last_sync_response | merge_dicts_with_defaults(last_heartbeat, last_password_set) }}"

- name: Display the final combined dictionary
  debug:
    var: combined_data
